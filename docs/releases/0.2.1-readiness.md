# 0.2.1 recovery release readiness

**Decision: v0.2.1 was authorized and fully qualified, but publication stopped
before upload. Preserve it and recover with a new fixed version.** This record
applies the [release checklist](../../checklists/RELEASE_READINESS.md) and
[failed-publication procedure](../MAINTENANCE.md) to the v0.2.0 provenance failure.

- Accountable owner: Josh Myers. Implementer: Codex.
- Recovery version/tag: **0.2.1** / **v0.2.1**.
- Preserved failed release: `v0.2.0` at
  `b36392b92b0f088bae7c96fafe97640b6a2648c9`, run `34786300295`.
- Original exact artifacts: wheel
  `e4a8d28aa14ce0f4caafea27d244aa4879a292c609cc8ec1ee8150d252a1c4a8`,
  sdist `02de0235f37b20e4f6e03d3ba8425bf50b7a5d7bd59da69f17fa81e8adcbc3ad`,
  manifest `19804f76020140c879ee2bd94086861ce0609f849a15dfb6c99a066899e73d16`.
- Scope and accepted residual risks were unchanged from
  [0.2.0](0.2.0-readiness.md). The six exact license decisions remain valid only
  through 2026-10-13 and only while dependency versions and distribution scope stay
  unchanged.

## Reconciliation and defect

- [x] The v0.2.0 workflow built the exact pair, passed all 12 Linux/macOS
  native/pandas/DPI installs and passed the fresh supply-chain audit.
- [x] `actions/attest` signed both exact subjects. The registered attestation's
  certificate and statement identify the expected repository, release workflow,
  tag, source/signer commit, release trigger, run attempt and GitHub-hosted runner.
- [x] Provenance verification failed before cryptographic verification because
  GitHub CLI 2.98 makes `--cert-identity` and `--signer-workflow` mutually exclusive.
  Running the retained pair with the exact certificate identity alone succeeds while
  preserving repository, source/signer digest, tag, OIDC issuer, predicate and hosted-
  runner constraints; authenticated statement policy remains unchanged.
- [x] The `publish` job was skipped. A post-run PyPI 0.2.0 release request returned
  HTTP 404. This does not authorize reuse: retain the public v0.2.0 tag and choose
  new 0.2.1 filenames as the recovery procedure requires.

## Recovery gates

- [x] Regression proves the verifier passes only one mutually exclusive actor selector
  while retaining the exact workflow/tag certificate identity and every other check.
- [x] The full local gate passes with unchanged numerical results, documentation and
  figure baselines. Repeat the supply-chain gate from the clean committed candidate;
  its intentional artifact/source-identity check rejects a dirty build.
- [x] PR and final-main exact-head CI pass all required jobs and artifact installations.
- [x] The v0.2.1 workflow builds, installs, audits, signs and independently verifies
  one new exact pair before the protected `pypi` environment.
- [x] Josh explicitly authorizes v0.2.1 publication and approves the concrete pair at
  the protected-environment review.
- [ ] Published hashes, metadata, provenance and fresh installations are verified.

Do not rerun v0.2.0, move its tag, weaken an identity constraint, use
`skip-existing`, or upload with a long-lived token.

## Release outcome

GitHub release `v0.2.1` triggered run `34787994313` at
`62854b83e0eb0048d53ab5a7f8bafcaf9b58ba76`. The locked build, all 12 clean
installations, supply-chain audit, signing and both independent provenance
verifications passed. The qualified wheel SHA-256 was
`da2b7895f81f22e7a617caf1409fe211f873e8467d092680d3ef3948ba1958d2`, the sdist
was `57dd9436470a760dc410a54499a23e1d59ab98f944421eb5bb5718bdc35be145`, and the
manifest was `ccedcd26afc5014ad5ae8a82175e64ca2281141b310d447a55daffb05428f6f5`.

After Josh approved the protected `pypi` environment, the pinned v1.13.0 publisher
rejected both artifacts before upload with `InvalidDistribution`: its metadata
reader did not accept their valid `Metadata-Version: 2.5`. The version-specific
PyPI endpoint returned HTTP 404 after the failed job. Do not rerun or move v0.2.1,
disable metadata verification, or reuse its filenames. See the
[0.2.2 recovery readiness record](0.2.2-readiness.md).
