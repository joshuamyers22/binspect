# 0.2.0 release readiness

**Decision: candidate preparation authorized; publication remains gated.** This is
an adapted [release checklist](../../checklists/RELEASE_READINESS.md). Josh Myers
selected 0.2.0 and instructed publication on 2026-09-13. That instruction authorizes
the release workflow after applicable gates; it is not recorded as independent
statistical review or as a blanket waiver of unresolved controls.

- Selected version: **0.2.0**, following the [compatibility allocation](../COMPATIBILITY.md).
  The release branch declares **0.2.0**. Existing 0.1.1 development artifacts cannot
  be relabeled or treated as tested 0.2.0 distributions; the workflow must build a
  new exact pair from the final tag target.
- Accountable release owner: Josh Myers. Qualified statistical reviewer: unassigned.
- Integrated baseline: main `184bb45`. Candidate branch: `release/0.2.0`.
  Exact future release commit: **pending the reviewed release PR merge**.
- Remote `main` observed at `184bb451341e04e815be4b1e97ee668b78d3d5fd`.
  [PR #30](https://github.com/joshuamyers22/binspect/pull/30) integrated the
  #24–#29 release-hardening stack with the prior A1–A3, D1–D3 and P1–P2 work.
  All 35 checks passed on exact integration head `30d0793`, including the
  supply-chain gate and 12 artifact installations. This integrated development
  evidence does not select or qualify a 0.2.0 candidate.
- Proposed scope: Python 3.10–3.13 on Linux/macOS; Polars-native preparation and
  default result tables; pandas inputs and explicit `.to_pandas()` compatibility.
  NumPy/SciPy estimation, descriptive FWL bins and the optional binsreg function
  target retain their separately documented limitations. No Python 3.14+, general
  few-cluster coverage, dark-background accessibility or general capacity claim.
- Accepted residual risk: six exact dependency-license scopes are accepted through
  2026-10-13 for separately installed packages not bundled in binspect artifacts.
  Any changed version or broader redistribution requires renewed review and the
  applicable upstream obligations. Every other unresolved item below is owned by
  Josh, with specialist review assigned where needed, due before the stated release step.

A read-only PyPI observation still reports latest version 0.1.1 with two unyanked
files. Their index-reported hashes differ from the R1 development pair bearing the
same filenames. The 0.2.0 endpoint returns 404; that is no retrievable release response,
not proof that a deleted filename has never been used. Exact observations are retained
in [R2 evidence](../evidence/release-readiness-2026-09-12.json). No published file was
replaced or downloaded as release qualification during this review.

## Available development evidence

| Evidence | What is verified | Limit for this release |
|---|---|---|
| [R3 local gate](../maintenance-recovery-review.md) | 600 unit/integration/reference tests, 94.65% coverage, development simulations, docs, figures and build | Development source at the recorded revisions; not a selected 0.2.0 release commit. |
| [R1 artifacts](../artifact-workflow-review.md) | One locked 0.1.1 pair, metadata/source verification, 12 local and 24 CI clean installations, actual upload/download handoff | These exact bytes are development artifacts. No 0.2.0 pair or published-install evidence exists. |
| [P3/R1 audit](../SUPPLY_CHAIN.md) | All 153 locked versions inspected; vulnerability/action/content/secret/SBOM checks passed at recorded revisions; Josh Myers accepted six exact license scopes on 2026-09-13 | Approvals expire 2026-10-13 and do not cover changed versions or broader redistribution. A fresh passing audit must match the actual release pair. |
| [R3 maintenance](../MAINTENANCE.md) | 222 locked/current references, three Polars/pandas/DPI journeys, ten simulated recovery decisions | Schedule/default-branch activation and actual PyPI operator recovery were not exercised. |
| [R2 renderer verification](../release-readiness-review.md) | Exact Python 3.12.14 obtained through uv; unchanged baseline comparisons pass locally and in CI at RMS 0.0 | Interpreter provisioning is repaired. Maintainer visual acceptance and candidate-specific qualification remain separate. |

All stored hashes identify their stated source/artifacts. They do not authenticate a
publisher or transfer a result from one version/commit/platform to another.

## Scope, correctness and compatibility gates

- [ ] **G1 policy/ADR acceptance.** Review the brief, ADR-0001's remaining proposals
  and the working agreement. ADR-0002 records the user's Polars choice. Record the
  accountable disposition; a merged implementation does not supply independent
  statistical or license approval.
- [ ] **G2 reporting/control acceptance.** Review the threat model and provide a
  verified private reporting route. The read-only API check still reports private
  vulnerability reporting disabled. Do not advertise an unverified private contact.
- [ ] **C1–C4 claim qualification.** Assign a qualified statistical reviewer, review
  the claim boundaries and decide the final-assessment protocol before consuming
  any reserved seeds. Adjusted-bin population coverage previously failed; public
  adjusted-bin uncertainty remains withheld. Uneven/few-cluster development failures
  remain explicit. Current arithmetic matches do not establish nominal coverage.
- [ ] **A1–A3 release compatibility.** Review the integrated migration and API/schema
  contract against the chosen release scope. Polars defaults, immutable arrays,
  verdict changes and rejected inference combinations require the proposed minor
  release; do not ship them as an unreviewed 0.1.2 patch.
- [ ] **D1–D3 release documentation/figures.** Confirm docs/examples/changelog match
  the final candidate, record visual acceptance, and keep dark-background limitations.
  Site hosting is separately unconfigured; documentation source is not a deployed site.
- [ ] **P1–P2 acceptance.** Review the measured workload envelope and accept or revise
  its baseline/budgets/controlled runner. The budget enforcement tool deliberately
  rejects pending acceptance. Requalify affected dependency profiles on the candidate;
  current resolution is dated monitoring, not a reproducible release lock.
- [x] **Version/changelog selection.** Josh selected 0.2.0 on 2026-09-13. The release
  branch updates `__version__`, release-facing status and changelog together; exact
  candidate build/test evidence is required before tagging.

## Repository, artifacts and operational gates

- [ ] **Select the exact candidate.** The #24–#29 stack is integrated through PR #30.
  Record the future candidate SHA, clean working tree, version and supported
  configuration, then require that candidate's own CI results. The integrated
  development head is not a selected release candidate or publication authorization.
- [ ] **Configure and verify actual controls.** `main` remains unprotected and rulesets
  empty. The PyPI environment requires Josh's review, permits self-review and has no
  deployment ref restriction. Review explicit required checks, bypass/review policy,
  allowed release refs and action controls before changing settings. Retest enforcement
  afterward; no remote settings were changed during this review.
- [ ] **Verify PyPI identity.** Inspect the owner-side Trusted Publisher mapping for
  project `binspect-regression`, owner `joshuamyers22`, repo `binspect`, workflow
  `release.yml`, environment `pypi`. Its actual mapping remains unverified here.
- [x] **Close P3 license decisions.** Josh Myers accepted the separately installed,
  non-bundled scope for binsreg 3.2.1, certifi 2026.7.22, docutils 0.23, fqdn 1.5.1,
  hypothesis 6.165.10 and pathspec 1.1.1 on 2026-09-13. The
  [registry](../../validation/supply-chain-exceptions.json) records exact scope,
  obligations, evidence and a 2026-10-13 expiry. Changed versions, broader
  redistribution or expiry require renewed review. The
  [six-package dossier](../LICENSE_REVIEW_DOSSIER.md) supplies the exact notices,
  advertised-extra/tooling paths and artifact evidence behind the decision.
- [ ] **Exact candidate CI and audit.** Run `make check`, all required candidate jobs,
  fresh online audit and applicable performance gates. Resolve failures rather than
  equating a local pass or a different PR head with candidate qualification.
- [ ] **Build, install and retain candidate distributions.** Build one locked pair,
  verify metadata/content/tag, inspect supported extras and native/optional clean
  installations, then retain immutable artifact IDs, manifest hash, wheel/sdist
  hashes, matching SBOM and audit results. Do not use mixed/stale root `dist/` files.
- [ ] **Verify build provenance.** Retain verifiable provenance for those exact files
  tying the subject digests to the reviewed source, workflow and CI run. Verify
  signer/workflow/repository/commit identity and retain the verification result.
  The current development manifest and checksums alone do not satisfy this item;
  no verified provenance for a 0.2.0 pair is available in this review.
  [Release provenance automation](../release-provenance-review.md) adds signing
  after qualification and independent verification before publishing; its synthetic
  rejection tests do not satisfy this candidate-specific gate.
- [ ] **Activate maintenance and qualify operator recovery.** After integration,
  run the manual workflow, verify actual retention and the first scheduled run.
  Review outstanding current/locked findings. Separately authorize and record the
  relevant operator recovery exercise; synthetic index reconciliation does not prove
  real PyPI permissions, yank behavior, partial OIDC retry or published recovery.
- [ ] **Authorize publication of the concrete pair.** Josh instructed publication
  of 0.2.0 on 2026-09-13, but the concrete pair and signed provenance do not exist
  until the release run. The required GitHub `pypi` deployment review is the final
  owner confirmation after upstream jobs identify and verify those exact artifacts.

## After authorized publication

- [ ] Read the version-specific index, verify actual file hashes, yank state and
  metadata, then fresh-install published artifacts/extras and run the affected
  synthetic journeys. Record index observation time and verified publisher/provenance.
- [ ] Retain release URL, artifacts, results, actual approver/date and any explicitly
  accepted scoped residual risks with owner and expiry. Update plan/memory and use
  the recovery procedure if publication or installed behavior differs from evidence.

## Next review order

1. Decide policy/statistical/performance/visual acceptance; verify the security-reporting
   route and proposed repository/PyPI controls, and keep the accepted license scope
   current through candidate qualification.
2. Activate maintenance, establish operator recovery evidence and select the actual
   0.2.0 candidate.
3. Qualify candidate CI, artifacts, audit and provenance, then request concrete
   publication authorization as the final step. Verify the published installation.

This record remains **not ready to trigger publication** until the unresolved
release-specific gates are satisfied or explicitly scoped decisions are recorded.
The [R2 review](../release-readiness-review.md) records what this preparation actually
changed and tested, without claiming completion of publication.
