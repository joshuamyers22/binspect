# Development and releases

The guide describes unreleased development behavior. Package version is 0.1.1;
the proposed next release is 0.2.0 because Polars return types, ownership and other
contract changes affect existing callers. This site is prepared for review and
has not been published. Hosting and maintainer publication approval remain open.

## Reproduce the documentation checks

Run from the repository root:

```sh
uv sync --frozen --all-extras
make docs
```

`make docs` executes every fenced Python block in the guide, README and A2/A3 contract
examples, with fresh processes and temporary output directories. It runs
`examples/quickstart.py` with an explicit temporary figure path, then builds the
site with `mkdocs build --strict`. The API reference is generated by mkdocstrings
from `src/binspect`, not a copied signature list. This is also the dedicated CI
`documentation` job and part of `make check`.

Strict configuration treats missing local pages/files, unresolved local anchors,
bad navigation and unrecognized relative links as warnings that fail the build.
External URLs are not fetched or certified by this gate. Generated API docstring
fragments are reference illustrations rather than the executable guide suite.
The generated site lives in ignored `site/`; build/check scripts do not publish it.

Preview locally with `uv run --frozen --all-extras mkdocs serve` and open the
address it reports. For the standalone synthetic figure, run
`uv run python examples/quickstart.py --output .work/quickstart.png`.
Updating `docs/hero.png` requires choosing that path explicitly.

The [seven-case synthetic gallery](guide/gallery.md) is also executable Markdown.
Its checkout launcher retains figures and a reproduction manifest:
`uv run --frozen --all-extras python examples/gallery.py --output .work/gallery`.
The launcher reads the page through the existing fence extractor, so teaching code
has one source. `make docs` executes it automatically in temporary storage. Replace
committed gallery images/manifest only after inspecting the generated files and
checking the documented interpretations; the check does not overwrite site assets.

## Package release is a separate action

The existing package workflow is triggered by a **published GitHub release**, not
by pushing a tag alone. It verifies that the release tag matches `v{__version__}`,
builds distributions, and uses the `pypi` environment and Trusted Publishing.
Follow the repository's
[release procedure](https://github.com/joshuamyers22/binspect/blob/main/RELEASING.md)
and readiness evidence before publication. The workflow is not changed by D1.

No Pages deployment or hosting configuration is introduced. Site publication
requires a passing build, configured hosting and explicit maintainer authorization.
The repository contracts linked from this guide describe development main after
integration; do not publish a draft branch's guide as released behavior.

## Evidence and open qualification

`make check` also runs lint/types/import contracts, unit/ownership/plot tests,
the minimal installation without pandas, locked optional-backend references,
development coverage simulations and distribution builds. Diagnostic coverage
failures remain visible; passing arithmetic/examples does not grant broader
inference validity. Reserved assessment seeds are not documentation examples.

CI is configured for Python 3.10–3.13 on Linux/macOS. P2 adds separate
[dependency configurations](guide/dependencies.md), including exact direct floors
and current compatible resolution. Other admitted combinations remain unqualified. The
[D2 export review](guide/figure-exports.md) adds four guarded PNG baselines and
structural PDF/SVG checks, with explicit background and simulation limits. The D3
gallery covers seven seeded teaching cases; review/integration remains pending.
[P1 workload measurements](guide/performance.md) cover the synthetic 10k–1M grid
and replace dense clustered-bin storage. Maintainer baseline/runner acceptance
remains pending. P3 adds a blocking online supply-chain gate; six exact license
scopes were accepted on 2026-09-13 through 2026-10-13. See the
[audit scope and decisions](https://github.com/joshuamyers22/binspect/blob/main/docs/SUPPLY_CHAIN.md).
R1 artifact/workflow and R3 recovery preparation are implemented; release-specific
qualification remains open. No general capacity or
accessibility qualification is implied.

| Repository reference | Purpose |
|---|---|
| [Contributing](https://github.com/joshuamyers22/binspect/blob/main/CONTRIBUTING.md) | Setup and checks |
| [Compatibility policy](https://github.com/joshuamyers22/binspect/blob/main/docs/COMPATIBILITY.md) | Supported combinations, migrations and release allocation |
| [Input/export contract](https://github.com/joshuamyers22/binspect/blob/main/docs/INPUT_OUTPUT_CONTRACT.md) | Shapes, alignment, table and JSON semantics |
| [Statistical analysis plan](https://github.com/joshuamyers22/binspect/blob/main/docs/STATISTICAL_ANALYSIS_PLAN.md) | Estimands, covariance and validation scope |
| [Project plan](https://github.com/joshuamyers22/binspect/blob/main/binspect-plan.md) | Remaining acceptance and release work |
